name: Exploring variables and secrets

on: 
    push
env:
    IMAGE_NAME: github-actions-nginx
jobs:
    docker:
        runs-on: ubuntu-latest
        steps:
        -  name: docker build
          
              
           run: echo docker build -t ${{var.DOCKER_REGISTRY}}/${{var.DOCKER_USERNAME}}/$IMAGE_NAME:latest

        -  name: docker login     
           run: echo docker login --username=${{var.DOCKER_USERNAME}} --password:${{secrets.DOCKER_PASSWORD}}

        -  name: docker publish
           run: echo docker push ${{var.DOCKER_REGISTRY}}/${{var.DOCKER_USERNAME}/$IMGAGE_NAME:latest

    deploy:
        runs-on: ubuntu-latest
        needs: docker
        steps:
        -  name: Docker run
           run: echo docker run -d -p 8080:80 ${{var.DOCKER_REGISTRY}}/${{DOCKER_USERNAME}}/$IMAGE_NAME:latest